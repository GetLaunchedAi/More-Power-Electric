---
name: Update Contact Page Body
overview: Update the `src/pages/contact.html` and `src/css/contact.css` files to match the layout and styling of the provided reference page.
todos:
  - id: update-html
    content: Update HTML structure in src/pages/contact.html
    status: pending
  - id: update-css
    content: Update CSS styles in src/css/contact.css
    status: pending
  - id: verify-design
    content: Verify responsiveness and dark mode
    status: pending
---

# Plan: Update Contact Page Body

This plan outlines the steps to update the contact page to match the design at [https://power-rentals--kr76xpcnn7.replit.app/contact](https://power-rentals--kr76xpcnn7.replit.app/contact).

## Proposed Changes

### 1. Update HTML Structure in [`src/pages/contact.html`](src/pages/contact.html)

- Modify the `#cs-contact-242` section to match the new layout.
- Update the contact form fields to include any missing fields from the reference (e.g., service type, address if applicable).
- Update the contact info section (Email, Phone, Hours, Address).
- Ensure the map `picture` element is correctly placed and styled.

### 2. Update Styles in [`src/css/contact.css`](src/css/contact.css)

- Adjust the layout of `#cs-contact-242` and its children (`.cs-container`, `.cs-content`, `#cs-form-242`, `.cs-right-section`).
- Update the styling for form inputs, labels, and buttons to match the reference.
- Update the styling for the contact info list (`.cs-ul`, `.cs-li`, `.cs-icon-wrapper`, `.cs-header`, `.cs-link`).
- Ensure the map container is styled correctly.
- Update dark mode styles to maintain consistency.

### 3. Review and Refine

- Compare the updated page with the reference to ensure visual accuracy.
- Check for any responsiveness issues across different screen sizes.

## Implementation Todos

- [ ] Update HTML structure in `src/pages/contact.html` [id: update-html]